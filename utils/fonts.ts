import { Raleway } from "next/font/google";

export const raleway = Raleway({
  subsets: ["latin"],
  weight: ["400"],
});

export const ralewayItalic = Raleway({
  subsets: ["latin"],
  style: ["italic"],
  weight: ["700"],
});
